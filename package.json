{
  "name": "admuse-easy",
  "version": "1.0.0",
  "description": "AI-powered marketing platform with business features",
  "main": "server/index.js",
  "scripts": {
    "start": "cd server && npm install && npm start",
    "build": "cd server && npm install"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}